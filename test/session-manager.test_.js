/**
 * SessionManager tests — معطّل (fca-unofficial مُزال)
 * fca-nx تدير الجلسات داخلياً عبر SQLite
 */
import { test } from 'node:test';
import assert from 'node:assert/strict';

test('placeholder — fca-nx manages sessions internally', () => {
  assert.ok(true, 'fca-nx uses its own session management');
});
