/**
 * persistent-fingerprint tests — معطّل (fca-unofficial مُزال)
 * انتقل إلى fca-nx التي لا تستخدم persistent-fingerprint
 */
import { test } from 'node:test';
import assert from 'node:assert/strict';

test('placeholder — fca-nx does not use persistent fingerprint', () => {
  assert.ok(true, 'fca-nx manages identity internally');
});
