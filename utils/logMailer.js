"use strict";

// logMailer is disabled — no email sending, no console interception.
function initLogMailer() {}
async function sendLogEmail() {}

export { initLogMailer, sendLogEmail };
export default { initLogMailer, sendLogEmail };
